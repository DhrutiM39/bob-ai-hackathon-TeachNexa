# CourseGenie AI — backend dependencies
# Install with: pip install -r src/backend/requirements.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9       # PostgreSQL driver (binary wheel — easy install)
alembic>=1.13.0              # migrations

# Settings / environment
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# AI provider
openai>=1.40.0               # OpenAI-compatible SDK (used for Gemini + legacy DeepSeek)

# Authentication
bcrypt>=4.0.0                # password hashing (used directly, avoids passlib compat issues)
python-jose[cryptography]>=3.3.0  # JWT tokens
email-validator>=2.1.0       # pydantic EmailStr validation

# Testing
pytest>=8.0.0
httpx>=0.27.0                # required by FastAPI TestClient
pytest-asyncio>=0.24.0
